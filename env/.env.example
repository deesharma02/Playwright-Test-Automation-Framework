# Copy this file to env/.env.dev, env/.env.qa, or env/.env.uat for local runs.
# In CI/CD, set these as secret variables or generate the target env file before running tests.

BASE_URL=https://tutorialsninja.com
API_URL=https://tutorialsninja.com/demo
APP_PATH=/demo/
APP_USERNAME=replace-with-ci-secret
APP_PASSWORD=replace-with-ci-secret
SEARCH_PRODUCT=HP LP3065
